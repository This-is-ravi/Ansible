- name: install packages
  hosts: web
  become: yes
  tasks:
  - name: install packages
    ansible.builtin.dnf:  # dnf to install packages
      name: "{{item}}"
      state: latest
    loop:
    - mysql
    - nginx
    - postfix
     


    